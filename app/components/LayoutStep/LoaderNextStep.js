import styled from 'styled-components'

const LoaderNextStep = styled.div`
  display: block;
  width: 24px;
  height: 24px;
  border-radius: 50%;
  border: 0.25rem solid rgba(255, 255, 255, 0.25);
  border-top-color: #ffffff;
  animation: spin 1s infinite linear;
`

export default LoaderNextStep
